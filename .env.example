# Relay server port (Vercel will override this)
PORT=8080

# Required: Secret token for authentication
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
BROTHERHOOD_SECRET=your_secret_here
